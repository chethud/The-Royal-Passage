/** Layout coordinates for passport overlays — tuned to 512×768 page assets. */
export const PASSPORT_PAGE_WIDTH = 512;
export const PASSPORT_PAGE_HEIGHT = 768;

export const PASSPORT_BOOK_WIDTH = PASSPORT_PAGE_WIDTH * 2;
